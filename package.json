{
  "name": "correctspell",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "clean": "rm -rf dist && mkdir dist"
  },
  "dependencies": {
    "nspell": "^2.1.5"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0"
  }
}
