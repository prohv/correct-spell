{
  "name": "correctspell",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Build completed. Ready for Chrome extension packaging.'",
    "clean": "rm -rf dist && mkdir dist"
  },
  "dependencies": {
    "dictionary-en-us": "^2.2.1",
    "nspell": "^2.1.5"
  }
}
